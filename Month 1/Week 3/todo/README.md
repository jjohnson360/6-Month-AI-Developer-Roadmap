# CLI To-Do List

A lightweight command-line task manager built in Python to practice state management, file persistence, and basic CRUD logic.

## Features
* View, add, and remove tasks interactively.
* Persistent storage using a local `tasks.txt` file so tasks remain saved between runs.
* Input validation for menu choices and index management.

## Usage
Run the script interactively:
```bash
python todo.py