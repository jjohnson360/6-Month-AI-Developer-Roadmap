# CLI Secure Password Generator

A command-line tool built in Python to generate customizable, secure passwords using native modules.

## Features
* Customizable password length with validation constraints.
* Optional inclusion of numbers and special symbols.
* Powered by Python's secure random generation libraries.

## Usage
Run the script interactively:
```bash
python password_generator.py